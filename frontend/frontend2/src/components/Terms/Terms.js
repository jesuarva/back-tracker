import React from 'react';
import { withRouter } from 'react-router-dom';

const Terms = () => {
  return (
    <div>
      <h1>Pending to be implemented</h1>
    </div>
  );
};

export default withRouter(Terms);
